// Fixed account-accent palette. Mirrors ACCOUNT_COLORS in api/_lib/mailAccount.js —
// the connect picker offers these swatches; rows/chips render the stored hex.
export const MAIL_COLORS = [
  "#e8930a", // dock amber
  "#3ba3ff", // minor blue
  "#12b886", // optimized green
  "#f0567a", // pink
  "#a06bff", // purple
  "#e0b341", // gold
];

export const DEFAULT_MAIL_COLOR = MAIL_COLORS[0];
